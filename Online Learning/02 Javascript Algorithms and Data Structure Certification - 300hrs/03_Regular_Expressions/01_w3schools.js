const obj = /w/.exec("The best things in life are free!");
console.log(obj)

//----------------------------------------------------------

const pattern = new RegExp("sear", "igm")
console.log(pattern)